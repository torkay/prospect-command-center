# =============================================================================
# Prospect Command Center - Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED: SerpAPI Key
# -----------------------------------------------------------------------------
# Get your key at https://serpapi.com/ (100 free searches/month)
# Pricing:
#   - Free: 100 searches/month
#   - Developer: $50/mo for 5,000 searches
#   - Production: $130/mo for 15,000 searches
SERPAPI_KEY=your_api_key_here

# -----------------------------------------------------------------------------
# OPTIONAL: Google Sheets Export
# -----------------------------------------------------------------------------
# To set up:
# 1. Go to https://console.cloud.google.com/
# 2. Create a project and enable the Google Sheets API & Google Drive API
# 3. Create a service account (APIs & Services > Credentials)
# 4. Download the JSON key file
# 5. Set one of the options below:

# Option 1: Path to credentials JSON file (local development)
# GOOGLE_SHEETS_CREDENTIALS_FILE=/path/to/credentials.json

# Option 2: JSON string (Railway/production deployment)
# GOOGLE_SHEETS_CREDENTIALS={"type":"service_account","project_id":"..."}

# -----------------------------------------------------------------------------
# OPTIONAL: Production/Railway Configuration
# -----------------------------------------------------------------------------

# Restrict CORS origins (comma-separated, default: * allows all)
# Example: ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
# ALLOWED_ORIGINS=*

# Custom database URL (auto-configured on Railway)
# Railway uses: sqlite:////data/prospects.db (persistent volume)
# Local uses: sqlite:///./prospects.db
# DATABASE_URL=sqlite:///./prospects.db
